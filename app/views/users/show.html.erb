<p id="notice"><%= notice %></p>

<div style = "margin:10px">
<p style = "margin: 10px">
  <strong>Name:</strong>
  <%= @user.name %>
  <strong>Reputation:</strong>
  <%= @user.reputation %>
</p>

<%= render @user.projects %>

<table>
	<tr>
		
		<td><%= button_to "Back", users_path, :class => "button inverse-dark tiny", :method => :get %></td>
	</tr>
</table>

</div>
