<p id="notice"><%= notice %></p>
<div style="margin:10px">
<!--
<p>
  <strong>User:</strong>
  <%= @project.user_id %>
</p>
-->
<p>
  <h1>Project: <% @project.name %></h1>
  
</p>

<p style="margin: 10px">
  <strong>Description:</strong>
  <%= @project.description %>



<dl class="tabs" data-tab>
	
	<dd class="active">
	<% @project.user.languages.each  do |language|  %>
  		<a href="#<%="langue" + language.name%>"><%= language.name%></a></dd><dd>
  	<% end %>
  	</dd>
</dl>
<div class="tabs-content">
	<% @project.user.languages.each do |language| %>
		<div class="content active" id="<%="langue" +language.name%>">
			<% @project.items.each do |item|%>
				<div class= "panel" >
					<h2><%= item.key %></h2>
					<% item.translations.each do |translation| %>
						<table>
							<% if translation.language_id == language.id%>
								<tr>
									<td><%= translation.value %></td>
									<td>Score: <span><%= translation.score %></span></td>
									<td><%= button_to "Vote up", vote_up_translation_path(translation), :class => "button tiny", :method => :put, remote:true %></td>
									<td><%= button_to "Vote down", vote_down_translation_path(translation), :class => "button tiny", :method => :put, remote:true %></td>
								</tr>
							<%end%>
					    </table>
					<% end %>
					<div><%= button_to "offer translation", new_item_translation_path(item.id), :class => "button inverse-dark tiny", :method => :get %></div>
					<%= render template: "translations/new"%>
				</div>
	  		<%end%>
	  </div>
	<% end %>
</div>



<%= form_for(@project, url: upload_items_project_path(@project), method: :put, remote: true) do |f| %>
    <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" />
    <%= f.file_field :new_items %>
    <%= f.submit 'Upload new items', :class => 'button inverse-dark tiny' %>
<% end %>


<%= button_to "Add item", new_project_item_path(@project), :class => "button inverse-dark tiny", :method => :get %>

<table>
	<tr>
		<td><%= button_to "Edit", edit_project_path(@project), :class => "button inverse-dark tiny", :method => :get %></td>
		<td><%= button_to "Back", project_path, :class => "button inverse-dark tiny", :method => :get %></td>
	</tr>
</table>

<%= link_to 'Add item', new_project_item_path(@project) %> |
<%= link_to 'Download iOS', download_ios_project_path(@project) %> |
<%= link_to 'Download Android', download_android_project_path(@project) %> |
<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>

</div>